import {saveLead,trackEvent} from './config.js';
export function mountLeadForm(result,onSuccess){const form=document.querySelector('#lead-form');if(!form)return;form.onsubmit=async e=>{e.preventDefault();if(!form.reportValidity())return;const submit=form.querySelector('[type="submit"]');const data={name:form.name.value.trim(),email:form.email.value.trim(),whatsapp:form.whatsapp.value.trim(),marketing:form.marketing.checked,privacy:form.privacy.checked};result.user={...result.user,firstName:data.name,email:data.email,whatsapp:data.whatsapp};submit.disabled=true;submit.textContent='Salvando com segurança...';try{await saveLead(data,result);trackEvent('lead_submitted');document.querySelector('#lead-gate').classList.add('hidden');onSuccess()}catch(error){console.error(error);const message=document.createElement('p');message.className='error';message.textContent='Não foi possível liberar o relatório agora. Verifique sua conexão e tente novamente.';form.append(message)}finally{submit.disabled=false;submit.textContent='Liberar relatório completo'}}}
